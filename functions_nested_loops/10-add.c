#include "main.h"
#include <stdio.h>
/**
 * add - Adds two integers and returns the result
 * @a: First integer
 * @b: Second integer
 *
 * Return: Sum of a and b
 */
int add(int n, int m)
{
return (n + m);
}
