#include "main.h"
#include <stdlib.h>
/**
 * abs - computes the absoilute of integeer
 * @n: the character to check
 * abs: finction to give absolute value
 * Return: int
 */
int _abs(int n)
{
return (abs(n));
}
