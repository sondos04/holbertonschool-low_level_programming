#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int _strcmp(char *s1, char *s2)
{
	int count1 = *s1[];
	int count2 = *s2[];
	int diff = count1 - count2;
	return (diff);

}
