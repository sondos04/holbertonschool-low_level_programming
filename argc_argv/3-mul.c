#include <stdio.h>
int main(int argc, char *argv[])
{
	int result;
	if (argc[] == NULL)
	{
		printf("Error");
		return (1);
	}
	else
	{
	result = argc[1] * argc[2];
	printf("%d\n", result);
	}
	return (0);
}
